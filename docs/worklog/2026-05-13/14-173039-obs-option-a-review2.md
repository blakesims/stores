## Review
- Correct: PASS. Prior blocker is fixed: `contract_state = "ready"` is treated the same as approved in the semantic row presentation (`src/tui/semantics.rs:227-228`) and the row renderer maps `contract-approved` to `next:promote/resolve` (`src/tui/render.rs:1195-1202`). The focused row regression test covers this exact ready case and verifies `▰ contract-approved`, `next:promote/resolve`, and no raw `contract:`/`tier:` fields (`src/tui/render.rs:2614-2636`).
- Correct: Observation top-card buckets are now mutually exclusive in `apply_obs_to_flow`: closed rows win first, contract gates (including `draft`, `approved`, and `ready`) increment only `ready`, non-ratification waiting increments only `waiting_kinds`, and remaining rows fall through to lifecycle buckets (`src/tui/data.rs:639-659`, `src/tui/data.rs:665-687`). Regression tests cover waiting-overlay and contract-gate exclusivity (`src/tui/data.rs:3568-3608`).
- Correct: Simplified observation rows hide raw duplicate fields. `format_obs_line` now renders a single semantic status, combines priority/tier as `priority/tier`, emits `next:<action>`, and does not append raw `tier:` or `contract:` tokens (`src/tui/render.rs:1152-1184`).
- Correct: Tests: targeted regression tests passed (`cargo test tui::data::tests::observation_flow_slots_are_mutually_exclusive_for_contract_gate -- --exact --nocapture`, `cargo test tui::data::tests::observation_flow_slots_are_mutually_exclusive_for_waiting_overlay -- --exact --nocapture`, `cargo test format_obs_line_surfaces_one_state_next_action_and_hides_raw_contract -- --nocapture`, `cargo test observation_contract_labels_are_semantic -- --nocapture`). A full `cargo test` run was attempted but exceeded the 180s review timeout while tests were still running; no failure was observed before timeout.
- Blocker: none found.
- Note: Gate PASS.
