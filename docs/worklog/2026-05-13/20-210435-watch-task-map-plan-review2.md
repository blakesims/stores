## Review
Gate: PASS

- Correct: The revised plan is anchored to the existing TUI projection/render path rather than a parallel frontend. It names `src/tui/data.rs`, `src/tui/semantics.rs`, `src/tui/render.rs`, and `src/tui/detail.rs` as the implementation seam, keeps `WatchProjection` as the broad grouping seam, and scopes rendering to the focused task path (`docs/worklog/2026-05-13/07-watch-task-map-implementation-plan.md:59-72`, `:223-227`). This matches the current code: `render.rs` already groups focused task rows by `task_watch_projection` and renders them through `format_row_line_for_task_projection` (`src/tui/render.rs:883-913`, `src/tui/render.rs:1293-1310`).
- Correct: The ready-state correction is present. The plan explicitly says durable execution must come from ADR fields (`lifecycle=active`, `active_step=coding`, `current_phase`, `current_cycle`) and legacy/status-only `ready` is fallback/implied only (`docs/worklog/2026-05-13/07-watch-task-map-implementation-plan.md:27`). Execution rules then source current coding/review cells from `active_step`, `current_phase`, and `current_cycle` (`:186-195`).
- Correct: `plan_review_log.length` semantics are now explicit: plan superscript comes from plan-review attempt count / `plan_review_log.length`, not `current_cycle` (`:54-57`, `:182`).
- Correct: Cycle grouping/latest-cycle precedence is specified. Historical execution cells group `cycles[]` by `phase`, choose max/latest `cycle`, render latest PASS/FAIL only from structured proof, handle REVISE followed by PASS, and let current executing/review state override a stale misleading result for the same phase (`:189-195`).
- Correct: Blocked/fault placement caveat is present. The plan allows `△`/`▲` outside the normal map when exact placement is unknown, requires dedicated proof before placing `▲` inside a phase, and forbids relying on the five newest `recent_events` for old failures (`:197-201`, `:288-295`). This is important because the current loader limits transition history to `LIMIT 5` (`src/tui/data.rs:1767-1803`).
- Correct: Typed serde parsing is included and actionable. Phase 1 adds typed `TaskPlanReviewEntry` and `TaskCycleEntry`, extends `TaskRow`, tolerates missing/null/malformed JSON, and preserves existing summary/default compatibility (`docs/worklog/2026-05-13/07-watch-task-map-implementation-plan.md:101-141`). This addresses the current flattening in `TaskRow`, where only summary vectors are stored (`src/tui/data.rs:184-203`, `src/tui/data.rs:1327-1335`).
- Correct: Exact/aligned table rendering acceptance is present. Phase 3 requires `ID | SUMMARY | MAP | REASON | AGE | TIER`, aligned headers, bounded reason, no prose/debug bags, dot preservation, superscripts, visible summary, and navigation alignment (`docs/worklog/2026-05-13/07-watch-task-map-implementation-plan.md:229-253`).
- Correct: Detail decode is present. Phase 4 requires decoding planning attempts, latest plan gate/source, each phase cell state/cycle/gate/source/confidence, fault source, and unknown/implied fields (`:261-269`).
- Note: One implementation detail to pin down during execution, not a gate blocker: Phase 2 names `wrap: Option<MapCell)` and tests `wrap/acceptance`, but gives less explicit source precedence for the wrap cell than for planning/execution. Use the same schema-truth discipline when implementing: e.g. source active wrap from `active_step=wrapping`, terminal/acceptance from persisted lifecycle/status/transition evidence, and avoid inferring a completed acceptance valve from phase passes alone.
