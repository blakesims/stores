## Review
- Correct: `src/tui/render.rs:262-285` replaces the old fixed per-card threshold with lane-specific minimum widths and uses `Constraint::Length` per lane, so the five-card branch is based on the actual summed label needs instead of `area.width / 5 >= 38`.
- Correct: `src/tui/render.rs:293-314` computes each lane's minimum from the actual six slot labels/meta, including multi-word labels via max word width, and `src/tui/render.rs:680-704` uses those lane-specific column minima when splitting each card's three columns.
- Correct: Unit coverage in `src/tui/render.rs:2371-2405` now paints at 140 columns and asserts all five lane titles plus readable slot words and no `+ more`; `src/tui/render.rs:2407-2464` covers fallback at 120 and 80 columns. `cargo test cockpit_top_strip -- --nocapture` shows those three unit tests pass in both `src/lib.rs` and `src/main.rs`.
- Blocker: REVISE — the focused test run still fails because the existing 140-column cockpit integration test was not updated for the new readable-label rendering. Evidence: `cargo test cockpit_top_strip -- --nocapture` fails `tests/tui_watch_cockpit.rs::cockpit_top_strip_paints_all_five_lane_labels_with_counts` at `tests/tui_watch_cockpit.rs:339-341`, expecting compact strings such as `"◌new1"`, while the new 140-column rendering correctly separates the glyph/count row from the label row (`"◌ 1"` and `"new"`). Exact fix: update `tests/tui_watch_cockpit.rs:321-337` to assert the new all-card/readable contract at 140 columns (lane labels, semantic labels, and relevant glyph/count markers separately) instead of obsolete compact abbreviations.
- Note: `git diff --cached --stat` is empty, so nothing is staged. The worktree contains many unrelated modified/untracked files; for this focused fix, staging should be limited to `src/tui/render.rs` plus `tests/tui_watch_cockpit.rs` if the integration-test expectation is revised.

Gate: REVISE
