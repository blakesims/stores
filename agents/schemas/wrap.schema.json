{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://stores.local/schemas/wrap",
  "title": "WrapEnvelope",
  "description": "Synthesis brief produced by the wrap agent at task completion. Persisted to wrap_log[].",
  "type": "object",
  "required": ["role", "executive_summary"],
  "additionalProperties": false,
  "properties": {
    "reasoning": {
      "type": "string",
      "description": "Free-text reasoning and synthesis notes before structured output (field-ordering recovery pattern)."
    },
    "role": {
      "const": "wrap",
      "description": "Identifies this envelope as the wrap agent output."
    },
    "executive_summary": {
      "type": "string",
      "description": "≤150-word summary: what was promised, what was delivered, key gaps."
    },
    "deviations": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Plan divergences that occurred during execution."
    },
    "residual_risks": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Risks that remain after task completion."
    },
    "recommended_sanity_checks": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Specific checks the reviewer should perform before GO/NO_GO."
    }
  }
}
