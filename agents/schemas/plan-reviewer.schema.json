{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://stores.local/schemas/plan-reviewer",
  "title": "PlanReviewerEnvelope",
  "description": "Structured output envelope for the plan-reviewer agent role.",
  "type": "object",
  "required": ["role", "gate", "summary"],
  "additionalProperties": false,
  "properties": {
    "reasoning": {
      "type": "string",
      "description": "Free-text reasoning and review notes before structured output (field-ordering recovery pattern)."
    },
    "role": {
      "const": "plan-reviewer",
      "description": "Identifies this envelope as the plan-reviewer output."
    },
    "gate": {
      "type": "string",
      "description": "Review gate decision: READY, NEEDS_WORK, etc."
    },
    "summary": {
      "type": "string",
      "description": "Human-readable summary of the review outcome."
    },
    "open_questions": {
      "type": "array",
      "description": "Questions requiring resolution before the plan is executable.",
      "items": {
        "type": "string"
      }
    }
  }
}
